<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :orderdate %>
    <%= f.date_select :orderdate %>
  </div>

  <div class="field">
    <%= f.label :netamount %>
    <%= f.text_field :netamount %>
  </div>

  <div class="field">
    <%= f.label :tax %>
    <%= f.text_field :tax %>
  </div>

  <div class="field">
    <%= f.label :totalamount %>
    <%= f.text_field :totalamount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
